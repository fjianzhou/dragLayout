
let configObj = {
  modularCount : 5,
  modularWidth : 5,
  modularHeight : 3,
  modularColumn : 12,
}

// 没有缓存数据是初始化GridItem位置信息
export const initGridItems = (option) =>{
  
  let gridItems = [];
  for(let i = 0 ; i< modularCount ; i++){
    // let x = 
    // let y =
    // if(){

    // }
    let gridRow = {
      x: x,
      y: y,
      w: option.width,
      h: option.height
    };
    gridItems.push(gridRow)
  }
  return []
}


export const initGridItem = () =>{

  
}